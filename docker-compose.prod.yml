name: kira-prod

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: kira-screener:prod
    container_name: kira-prod
    restart: unless-stopped
    env_file: .env.production
    environment:
      APP_ENV: production
      GIT_COMMIT: ${GIT_COMMIT:-unknown}
    ports:
      # Loopback only; Cloudflare Tunnel provides ingress.
      - "127.0.0.1:8080:8000"
    volumes:
      # Writable: POST /brain/activate rewrites active_version.json in place, and
      # that is the hot-swap. The app writes nothing else here.
      - ./company_brain:/app/company_brain:rw
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/health').status==200 else 1)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
