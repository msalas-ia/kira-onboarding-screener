# Company Brain
Authoritative compliance knowledge base the agent consumes at runtime.
- `screening_policy.md` — the decision policy (authoritative; overrides base instructions).
You may add more docs here (jurisdiction lists, escalation runbooks). The agent must treat this
folder as the source of truth. In the live call you will be asked to ADD a new authoritative rule here
and show it overrides behavior deterministically.
